const convertDuration = (yt_duration) => {
    // const time_extractor = /([0-9]*H)?([0-9]*M)?([0-9]*S)?$/;
    // const extracted = time_extractor.exec(yt_duration);
    // const hours = parseInt(extracted[1], 10) || 0;
    // const minutes = parseInt(extracted[2], 10) || 0;
    // const seconds = parseInt(extracted[3], 10) || 0;
    // return hours * 3600 * 1000 + ':' + minutes * 60 * 1000 + ':' + seconds * 1000;
    // const initHours = moment.duration(yt_duration).asHours();
    // const initMinutes = moment.duration(yt_duration).asMinutes();
    // const initSeconds = moment.duration(yt_duration).asSeconds();
    // const hours = Math.floor(initHours);
    // if(hours > 0){
    //     const minutes = initHours %
    // }else{
    // }
};

export default convertDuration;
